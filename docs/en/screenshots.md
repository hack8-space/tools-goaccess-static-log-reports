# Screenshots and example output

The project generates a custom static landing page and one regular GoAccess report per domain.

## Landing page

The landing page is written to:

```text
/opt/goaccess/static-log-reports/html/index.html
```

It automatically links to all domains configured in `DOMAINS=(...)`.

## GoAccess report

Each domain report is generated by GoAccess itself and written to:

```text
/opt/goaccess/static-log-reports/html/<domain>/index.html
```

Example:

![Example generated GoAccess report](../assets/goaccess-report-example.png)

The screenshot shows aggregated statistics. For public repositories, do not use screenshots that expose real IP addresses, credentials, internal hostnames, private query parameters, or sensitive referrers.
